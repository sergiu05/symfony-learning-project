{% if show_confirmation|default(false) %}
	{% set attr = {'data-confirmation': 'true'} %}
	{{ include('admin/_confirmation_delete.html.twig') }}
{% endif %}

{{ form_start(form, {attr: attr|default({}) }) }}
	{{ form_widget(form) }}

	<input type="submit" value="{{ button_label|default('Submit') }}" class="{{ button_css|default("btn btn-primary") }}">
{{ form_end(form) }}
